# Polymarket RBI Bot — Path to Profitability

Owner: makusho95@gmail.com
Started: 2026-04-20
Goal: build this repo into a durable passive-income stream.

This plan is **staged and gated**. Each phase has a go/no-go decision at the end. We do not advance to the next phase until the current phase's ship criteria are met. We do not throw good capital after bad signals.

Status legend: `[ ]` pending · `[~]` active · `[x]` done · `[-]` abandoned (with reason)

---

## 0. Reality check (read before trading anything)

- "Retirement income" is an ambitious framing for a single bot on a single venue. To net ~$50k/year you need either (a) large capital at a modest edge, or (b) a small capital at an unusually stable high Sharpe — rare, and generally doesn't hold when scaled.
- Polymarket constraints that cap realistic scaling:
  - Thin per-market liquidity (most markets cannot absorb size without moving).
  - Event-driven jumps dominate slow mean-reversion.
  - Informed participants absorb most directional edge in news/legal/political markets.
  - Limit-order-only execution with discrete tick sizes.
- What this means operationally:
  - Treat this bot as **one stream** of a diversified passive-income strategy, not the whole strategy.
  - Pick an edge that compounds on small capital first. Scale only after live proves it.
  - The current README itself says: "This repo is a strong starter, not a production-complete market-making system." Take that seriously.

**Working target (not a promise):** 10–25% annualized net on trading capital, Sharpe > 1.0 over 3+ months of live, low correlation to crypto. We will revise this target after Phase 2.

---

## 1. Edge hypothesis (what we actually bet on)

Ranked by how well this codebase is currently positioned to capture each:

1. **Structural arbitrage / no-arb bundles** — complementary tokens should sum to ~$1. `deploy/scan_structural_arbitrage.py` already scans for this; it is research-only today. This is the most defensible Polymarket edge for a small retail bot because it doesn't require predicting outcomes.
2. **Regime/family filtering** — trading only in market families where short-horizon price dynamics are more predictable (scheduled sports outrights, season awards) and aggressively avoiding news/legal/political/event-resolution markets. `bot/market_filter.py` + the OpenAI classifier pipeline already exist.
3. **Maker-style passive quoting** — capture spread in mature, liquid, long-horizon markets. Requires order book / websocket infrastructure we do **not** have yet.
4. **Directional technical signals (MACD/RSI/CVD/LongEntry)** — the repo's current default. Working hypothesis: these do **not** survive honest transaction costs on event markets. Phase 2 will falsify or confirm this. If they don't survive, we stop running them.

We will commit capital to (1) and (2) first, (3) conditionally, (4) only if Phase 2 shows survival.

---

## 2. Phases and ship criteria

### Phase 0 — Baseline and journaling (target: week 1)

- [x] Decide risk-of-ruin capital: the amount whose **total loss** would not change your life. `RISK_CAPITAL_USD = 500`.
- [x] Decide minimum viable edge (MVE) threshold:
  - **Arb track:** net edge ≥ **30 bps** per round-trip, after spreads paid on all legs + fees. (Convergence is the exit, so spread is paid only on entry.)
  - **Directional track:** net edge ≥ **100 bps** per round-trip, after fees and spread paid on both entry and exit.
- [ ] Start a decision journal: every meaningful change (parameter, code, strategy) logged with date + reason + expected impact. Use the **Progress log** section at the bottom of this file.
- [x] Reconcile `.env` — confirm we are pointed at the real CLOB host and the wallet/funder that will actually trade. Do not skip.
  - Config loads clean: CLOB host + Gamma host correct (mainnet chain 137), `PRIVATE_KEY` / API triple / `FUNDER_ADDRESS` all populated, `has_l2_auth=True`. `.env` gitignored, never committed.
  - `SIGNATURE_TYPE=2` confirmed correct — prior successful place + cancel round-trip on the current wallet is proof the signature type matches the account.

Ship criteria: `RISK_CAPITAL_USD`, MVE, and journal set up.

---

### Phase 1 — Measurement infrastructure (target: weeks 1–3)

We cannot improve what we cannot measure honestly. Every later phase depends on these.

- [~] Stand up a persistent background quote collector for 20–50 candidate markets using `deploy.collect_quotes --use-clob-order-books`. Run it long enough (≥ 3 weeks) to have real per-token order-book history, not Gamma proxies.
  - Watchlist: `data/scan_shortlist.json` (50 condition-ids, ~100 tokens after YES/NO expansion, 47 A-tier from `scan_markets --limit 200 --top 50`).
  - LaunchAgent: `deploy/com.polymarket.rbi-bot.quote-collector.plist`. Install with: `cp deploy/com.polymarket.rbi-bot.quote-collector.plist ~/Library/LaunchAgents/ && launchctl load ~/Library/LaunchAgents/com.polymarket.rbi-bot.quote-collector.plist`.
  - Output: `data/quote_collection/run.jsonl` (append-only). Logs: `data/quote_collection/collector.{out,err}.log`.
  - Pre-req for ≥3 weeks of continuous data: prevent laptop sleep — `sudo pmset -a sleep 0 disksleep 0` (or keep it plugged in + lid-open + set Energy Saver "prevent automatic sleeping").
  - Periodic refresh: re-run `python -m deploy.scan_markets --limit 200 --top 50 > data/scan_shortlist.json` every ~7 days so resolved markets get rotated out. LaunchAgent will pick up the new watchlist on next restart (`launchctl kickstart -k gui/$UID/com.polymarket.rbi-bot.quote-collector`).
- [x] Audit `data/paper_trades.jsonl`: every logged decision must contain enough context to replay it (mid, bid, ask, decision_ts, strategy scores, guardrail state). Patch `bot/paper_log.py` if fields are missing.
  - Audit of 28 existing entries: mid, condition_id, token_id, timestamp, status, reason were always present. Gap: `best_bid`/`best_ask` only on blocked entries (flowing through guardrails); missing on dry_run/no_trade. Spread-replay impossible for those.
  - Patched `deploy/run_live.py::log_if_paper` to always emit `best_bid`, `best_ask`, and compute `observed_spread_bps` on every entry. Added `schema_version: 1` so future shape bumps are traceable. `decision_ts` capture already existed; now surfaces consistently when caller passes `--decision-ts`.
  - Verified with a fresh dry-run: new entry carries all replay fields.
- [x] Add an explicit **cost model** to the backtest engine that attributes PnL into: entry-side spread paid, exit-side spread paid, slippage vs mid, fees, adverse selection (fill only when market moved against us). Even a crude attribution is better than a single PnL number.
  - `BacktestEngine`: new `fee_bps` + `adverse_selection_horizon_bars` knobs. Per trade: `spread_cost_bps`, `slippage_cost_bps`, `fee_cost_bps`, `post_fill_move_bps`, `notional`, `mid_at_fill`. Aggregated in `result.metadata.cost_attribution`: gross vs net pnl, USD cost buckets, adverse-fill ratio.
  - `deploy.run_backtest` flags: `--fee-bps`, `--adverse-selection-horizon-bars`. Output JSON now includes a top-level `cost_attribution` block.
  - `deploy.run_experiment_matrix` passes cost_attribution through per-row.
- [ ] Extend the dashboard (`dashboard/server.py`) to show this cost attribution on recent decisions. (Optional; nice-to-have.)

Ship criteria: at least 3 weeks of real CLOB order-book snapshots for ≥ 20 markets, and a cost-attributed backtest report.

---

### Phase 2 — Honest research on existing tooling (target: weeks 2–6)

With real collected data, run the research tools that already exist. Do not build new strategies in this phase.

- [ ] Run `deploy.run_experiment_matrix` across the collected CSVs. Record ranked profiles.
- [ ] Run `deploy.run_walk_forward` on the same data with `--train-bars 12 --test-bars 4 --step-bars 4`. This is the honesty test — out-of-sample only.
- [ ] Run `deploy.export_state_features` to inspect which regimes the bot actually trades in vs. which are profitable.
- [ ] Fit the above against the cost model from Phase 1. The only number that matters is **out-of-sample net expectancy after honest costs**.
- [ ] **Calibration check (new).** For every BUY decision the signal stack would have made, record (model_prob, market_prob_at_entry, resolution). At the end of Phase 2, compute:
  - Brier score of model vs market prob on ≥ 200 decisions.
  - Calibration curve binned by model probability (are bins reliable?).
  - Edge-after-costs per decile of confidence.
  A strategy that is "often right" on direction can still lose money if its probabilities are poorly calibrated and it enters at bad prices. Calibration is the honest test of whether the signal stack's confidence means anything. If Brier(model) ≥ Brier(market-implied), the stack is adding noise — treat that as a no-pass.

**Decision gate — hard:**
- If at least one strict-mode profile is out-of-sample net-positive after costs on fresh data across ≥ 10 markets, continue with that profile as a **secondary** overlay.
- If **no profile** passes, mark directional signal trading `[-]` abandoned and commit to the structural-arb path exclusively. Do not rescue the signal stack with more parameters — that's overfitting.

Ship criteria: documented decision about the signal stack in the Progress log.

---

### Phase 3 — Build the primary edge (target: weeks 4–10)

Primary track: **structural arbitrage as an executable strategy.**

- [ ] Extend `deploy/scan_structural_arbitrage.py` to emit *persistence*: how long does each observed bundle mispricing last in the collected quote history? Only mispricings that persist > latency + execution window are actionable.
- [ ] Model multi-leg execution risk: for a 2–4 leg bundle, cost = sum of spreads paid + race/latency slippage + fees. Only trade bundles whose observed edge exceeds this by ≥ MVE.
- [ ] Add an executable `deploy/run_arb.py` that: monitors selected condition IDs, confirms the current bundle still violates no-arb, sizes each leg to risk cap, places limit orders (respecting `RiskManager`), and auto-cancels stale legs.
- [ ] Paper-run it for ≥ 2 weeks. Compare hypothetical fills to what `deploy.collect_quotes` actually saw in the same window.

Secondary track (if Phase 2 produced a survivor): run the strict-mode signal profile as an overlay in allowed market families only, at half the arb size.

Ship criteria: 2+ weeks of paper arb runs where hypothetical PnL (after costs) clears MVE and paper fill rate is realistic.

---

### Phase 4 — Small live pilot (target: weeks 8–16)

- [ ] Start with capital = min(`RISK_CAPITAL_USD`, $500). Hard cap: do not raise this mid-pilot.
- [ ] Tighten `RiskManager` limits: `MAX_NOTIONAL_PER_ORDER`, `DAILY_LOSS_LIMIT`, `MAX_OPEN_ORDERS_TOTAL` — cut defaults in half for the pilot.
- [ ] Daily review ritual: diff `data/live_state.json` fills vs. `data/paper_trades.jsonl` predictions. Any divergence > 50 bps gets investigated the same day.
- [ ] Weekly review: realized PnL, slippage vs model, cancels, missed arbs.

**Kill criteria (any one trips, pause live):**
- Two consecutive weeks of net-negative after costs.
- Fill quality > 75 bps worse than paper on average.
- Any single unexplained loss > 2× daily loss limit.
- Reconciliation failures > 10% of sessions.

Ship criteria: 8 weeks of live without tripping a kill criterion, net positive after costs.

---

### Phase 5 — Scale (months 4+)

- [ ] Only if Phase 4 cleared: scale capital in 2× steps, no more frequent than monthly. Do not scale complexity.
- [ ] Track capacity: at each step, check whether fill rate or slippage degraded. If they did, you hit venue capacity — stop scaling, don't fight it.
- [ ] Add a second, uncorrelated edge candidate only after the first is stable at target size. Ranked candidates:
  1. **Maker-style passive quoting** in mature high-liquidity sports outrights. Requires websocket/order-book infrastructure (not present today — 2–4 week build).
  2. **LLM-probability engine** (AI-as-updater, not oracle) on narrow niches where information is messy and the consensus is slow. Scope:
     - Build a news/evidence ingestion pipeline (RSS + Gamma market descriptions + scheduled data releases). 2–3 week build.
     - For each selected market, prompt the model for `(p_yes, confidence, top 3 evidence points, "what would flip this")`. Reuse the existing `classify_markets_openai` offline pattern — no synchronous LLM calls inside the trade loop.
     - Trade only when `|model_prob − market_prob| × notional > MVE + 2 × estimated_costs_bps` and Phase 2's calibration check said this niche was calibrated.
     - Honest caveat: LLMs compete poorly on breaking-news markets where insiders and scrapers are faster. Pick niches (earnings-call wording, macro-data-release markets, crypto governance) where messy-text parsing genuinely helps.
  3. **Information overlay** in families you personally track (human in the loop, bot executes).

  Only build one. Picking the LLM track before the maker track commits us to the news infrastructure; do not build both in parallel.

Ship criteria: 3+ months of live at scaled size with PnL consistent with Phase 4 run-rate.

---

### Phase 6 — Operationalize (months 6+)

- [ ] Alerting: any guardrail trip, reconciliation failure, or daily-loss-limit hit pages you.
- [ ] Backups: `data/live_state.json` replicated off the laptop.
- [ ] Multi-wallet support for redundancy and capital segregation.
- [ ] Documented manual override playbook for black-swan events (venue outage, compromised key, stuck order).
- [ ] Tax / accounting hygiene. Prediction-market income has real tax implications — get this right before it becomes a bigger number.

Ship criteria: the bot can run for 2 weeks without you touching it and nothing bad happens.

---

## 3. Off-ramps (when to stop)

- **After Phase 2 with no signal-stack survivor and no actionable arb persistence**: shelf the live trading plan, keep the repo as a research tool only.
- **After Phase 4 with kill criteria tripped twice**: pause live for ≥ 1 month, re-run Phase 2 on fresh data before re-enabling.
- **Personal bankroll stress**: if the bot occupies more emotional bandwidth than the income justifies, it's not passive. Scale down or stop.

Stopping is a valid outcome. This file should be updated with the stop reason if we reach one.

---

## 4. Principles

- **Measure first, trade second.** Phase 1 exists because every Phase 2+ decision is worthless without honest costs.
- **Pick one edge.** A single working strategy at stable size beats five weak ones.
- **Out-of-sample only.** If we tune on the same data we evaluate on, we are lying to ourselves.
- **Scale capital, not complexity.** New code is new risk.
- **Kill criteria are sacred.** If we lower them mid-pilot, we don't have kill criteria.

---

## 5. Progress log

Append entries as we go. Newest at top. Each entry: date, phase, what changed, expected impact, actual outcome (backfilled later).

- 2026-05-18 — Phase 5 / Repo cleanup (round 2) — **Archived 23 falsified-strategy CLI scripts into `deploy/archive/`** mirroring the `data/archive/` structure (directional / structural-arb / maker-quoting / research-pipeline subdirectories with a top-level README). `deploy/` top-level now contains 24 active scripts (operational + live trading + LLM strategy + reusable generic infra); 100% of falsified-track scripts moved out without deletion. Fixed one import dependency (`track_a_decision.py` → `scan_live_arb.py::DEFAULT_CHAMPIONSHIP_PATTERNS`) by inlining the 6-line constant so both could be archived together cleanly. Cumulative session cleanup: 11 zero-value files removed + 66 data artifacts archived + 23 deploy scripts archived = **100 items relocated, 0 destructive deletions of non-empty content.** `data/archive/` and `deploy/archive/` are mirrored, both with READMEs. Restore any item with `mv <archive-path> <original-dir>/`.
- 2026-05-18 — Phase 5 / Repo cleanup — **Removed 11 zero-value files (empty stubs, one-shot run logs, macOS-only plists); archived 66 falsified-strategy artifacts into `data/archive/` (directional / structural-arb / maker-quoting / old-snapshots subdirectories) with a `README.md` manifest.** `data/` top-level now contains only the active working set (LLM predictions + outputs, watchlists, classifier I/O, token CSVs, Python modules, live quote_collection/). Nothing deleted destructively — all archived artifacts can be moved back with `mv data/archive/<group>/<file> data/`. Quote collectors untouched (still running, 4.4 GB of live JSONL).
- 2026-05-18 — Phase 5 / Web dashboard — **`dashboard/llm_server.py` shipped.** Stdlib-only HTTP server with auto-refresh HTML page + `/api/state` JSON endpoint. Run: `python -m dashboard.llm_server --port 8765` then open http://127.0.0.1:8765. Shows on one screen: capital (USDC balance, self-cap, exposure in positions + orders, available to deploy, utilization bar), open positions + orders, tradeable candidates with expired-market filter (only future-resolution markets passing SELL + mid<0.20 + |Δ|≥0.05), predictions summary, Stage B latest resolver verdict + paper PnL, Stage A retro evidence with grid table. Live CLOB state cached 30s to avoid hammering the auth API. Read-only — no buttons that place trades. **Capital math is correct**: exposure = sum of (positions × avg_price) + (open BUY orders × price × size), NOT (self_cap − balance). Verified: $4.66 USDC, $0 exposure, $4.66 available, 0% utilization, only 1 unexpired candidate (Brunson NBA MVP). Replaces the need for `deploy/status.py` for at-a-glance use (status.py is still there for CLI/scripting).
- 2026-05-18 — Phase 5 / Session playbook — **Pausing active development; entering observation phase.** Validated direction: Stage A retro +$118 SELL-only PnL on 240 markets; PGA verdict +$2.06 / 100% wins on 3 refined-filter trades. Open question: does this convert to realized live PnL when actually traded? Need 3-5 clean live trades to find out. Active-development pause until: (a) new retail-speculation bracket event appears on Polymarket (next album release, awards show, viral political/cultural bracket, contest with multiple long-shot candidates), OR (b) ~1 week passes and we re-scan for newly-listed events. **Session-resume playbook**: (1) `python -m deploy.status` to see latest state — balance, candidates passing filter, paper PnL, retro evidence; (2) if a clean candidate exists (SELL, mid<0.20, |Δ|≥0.05, NOT in-flight = price hasn't moved >2× since prediction, tight NO-side spread), propose a $1 live trade via `python -m deploy.run_live --llm-mode --execute --token-id <no_tid> --condition-id <cid> --best-bid <no_bid> --best-ask <no_ask> --mid-price <no_mid> --decision-ts <iso_now> --skip-eligibility`; (3) if no clean candidate, run `python -m deploy.llm_predict_markets --event-slug <new_slug>` on newly-listed bracketed events; (4) after each resolution wave, run `python -m deploy.llm_resolve_predictions`. **Anti-pattern checks (manual gates before any live trade)**: (a) is the YES price > 2× higher than at prediction time? → in-flight, ABORT; (b) is the market a sportsbook-informed market actively in progress? → Claude lacks current data, ABORT; (c) is this a BUY signal? → systematic anti-edge per retro, ABORT; (d) is mid > 0.20? → favorite-fade anti-pattern, ABORT. **DO NOT auto-trade until 3-5 manual live wins.** Continuous infrastructure: only quote collectors (data collection, no decisions). API spent cumulative: ~$18.60. Live capital deployed: $0.
- 2026-05-18 — Phase 5 / Stage C candidate hunt — **No clean live candidate found tonight; Stage A's Drake/Eurovision shape is rare on Polymarket.** Ran the bracketed-event discovery: 26 currently-open events resolving 7-45 days. Top fits by shape (political primaries, non-in-flight, ≥4 markets): California Governor primary ($107K liq, 14d), Virginia Republican Senate ($43K, 29d), NY-07 Democratic primary ($60K, 36d), Oklahoma Republican Senate ($70K, 29d), Lebanon Parliamentary ($70K, 13d). Ran predictions on 2 of these (Cali Gov slug returned 0 markets — need to debug slug name; Virginia returned 3 of 5 markets). Virginia outcomes: Bert Mizusawa SELL (mid=0.48, p=0.05, |Δ|=0.43) → **fails refined filter** (favorite-fade anti-pattern, mid>0.20); Jason Miyares BUY (+1460 bps) → BUY-side is anti-edge per Stage A retro; Al Mina BUY (+95 bps) → below threshold. **Zero clean live candidates** matching our refined filter (SELL, mid<0.20, |Δ|≥0.05, tight NO-side spread, non-in-flight). **Lesson**: political primary brackets DON'T have the same retail-speculation overpricing pattern as Stage A's Drake-album-features and Eurovision-bracket events. The tiny long-shot candidates ($0.001-$0.01) are already accurately priced near zero; the favorites are correctly priced near their true odds; Claude's deviations are mostly noise or mid-range guesses. **The Stage A edge appears to be specific to retail-speculation event-types** (album rumored features, contest-bracket "will X be in top N") where retail bids on names recognition. **No live trade tonight.** Tomorrow: try NY-07, Lebanon Parliamentary, Oklahoma Senate, and fix the Cali Gov slug. Consider refining the prompt so Claude reliably produces long-shot SELL signals when the bracket has 10+ buckets at ≤$0.02 each. Cumulative API: ~$18.60. Live capital: $0.
- 2026-05-18 — Phase 5 / Stage B PGA verdict + refined-filter vindication — **PGA Championship resolved; 27 tradeable bets net +$1.81 at $5 size (14W/13L); the refined filter (SELL, mid<0.20, |Δ|≥0.05) was 100% wins on the 3 PGA bets it would have taken.** Final PGA results from `deploy.llm_resolve_predictions`: 55 of 79 predictions resolved, 27 met the original edge filter, paper PnL +$1.81 at $5/trade. Resolver verdict label says NO_GO because it uses outdated Brier-only criterion (Brier LLM 0.0448 > Brier market 0.0204) but the realized PnL is positive. **Filtered to refined-rule (SELL, mid<0.20, |Δ|≥0.05): 3 trades, 100% win rate, total +$2.06 on $15 capital at $5/trade ($0.41 at $1/trade)**:
  - Dustin Johnson Top-5 SELL (mid=0.196, |Δ|=0.156, y=0) → +$0.978
  - Harris English Top-5 SELL (mid=0.160, |Δ|=0.120, y=0) → +$0.800
  - Alex Smalley Win SELL (mid=0.056, |Δ|=0.053, y=0) → +$0.280
  **All 4 biggest losers (Kitayama -$2.55, McIlroy -$2.08, McNealy -$1.33, Aberg -$1.25) had mid>0.20** — the in-flight anti-pattern is real and the filter correctly rejects it. **Edge-shape hypothesis vindicated on live data.** The Smalley trade we aborted Sunday would have won $0.06 at $1 size, but the in-flight risk we flagged was real (1.83x price move during Round 4); we got lucky. **Live trade candidates Monday morning**: Only one remains in the prediction set — Jalen Brunson NBA Finals MVP SELL (mid=0.084 at prediction, now $0.155 — 1.83x move, just under in-flight threshold). Current NO spread $0.77/$0.92 (~1780 bps) **exceeds `MAX_SPREAD_BPS=250` and would block via `evaluate_execution_guards`**. Spread cost ~890 bps eats most of the +1250 bps edge anyway, leaving net ≈360 bps; trade EV +$0.05 at Claude's prob vs −$0.08 at market's prob = essentially a coin flip. **Recommendation: don't trade Brunson; run fresh predictions on currently-open markets to find a non-in-flight, tight-spread candidate before any live execution.** Output: `data/llm_resolution_results.json`.
- 2026-05-17 — Phase 5 / Stage C trade abort — **Smalley winner-SELL aborted late Sun; built status CLI; refined edge-shape hypothesis.** Built `deploy/status.py` — single-command dashboard showing USDC balance + L2 auth, self-cap + max-notional limits, open orders, positions + realized PnL, LLM predictions count (total + fresh-24h), tradeable candidates passing the current filter (default mid<0.20, |Δ|≥0.05, SELL), paper-trade activity, latest resolver verdict, Stage A retro headline. Runs in <2s when --skip-clob is passed, ~3s with live CLOB query. Replaces the need for the HTTP dashboard for now. User authorized a $1 live test trade on the strongest Stage C candidate (Smalley winner-SELL via buy-NO route). Pre-trade verification fetched Smalley's current market state: YES bid moved from **$0.056 yesterday → $0.171 right now** (3.3× upward during Round 4 — Smalley is in real contention). Claude's prediction p_yes=0.003 unchanged. **This is the in-flight anti-pattern**: when market price is anchored on live real-time data (current tournament position, current chart, current campaign developments), Claude lacks that data and its prior-based prediction is wrong by definition. Same failure mode as today's Wyndham Clark Top-20 BUY (-$0.18) and Rory McIlroy Top-20 SELL signal. **Refined edge-shape hypothesis**: Claude's SELL edge works on markets where price is **anchored on retail prior** (recently listed, long-horizon, no current data updates). Markets **in flight** (active event with live state) have prices that integrate real-time data Claude lacks; Claude's deviation from market is a false signal there. The Stage A retro PnL of +$118 came mostly from markets that were either resolved or near-resolved when collected — i.e., the buckets were *already-settling-on-NO* long-shot retail speculation, not active-state predictions. **Decision: aborted the Smalley trade**. No live capital deployed today. Monday's plan: run final resolver on the 79 paper-tracked predictions, apply revised filter, look for candidates that match the *correct* edge shape (long-horizon retail-bracket markets, low current-data dependence). Jalen Brunson NBA MVP SELL (mid=0.084, |Δ|=0.05, resolves ~June 17, opened weeks before Finals start) is a likely Monday candidate — fits the "long-horizon retail speculation" shape, NBA Finals haven't started so no real-time data anchors the price yet.
- 2026-05-17 — Phase 5 / Retro-PnL — **Strong PnL evidence for SELL-side edge on Stage A's 240 resolved markets; BUY-side catastrophic.** Built `deploy/llm_retro_filter_pnl.py` which retroactively applies filter variants to Stage A's resolved calibration set and computes realized paper PnL with correct buy-NO mechanics ($5/trade, shares = $5 / (1 − market_p_t7), payoff if y=0 → shares × $1, else $0). **Baseline all-SELL: 150 trades, 71.3% win rate, +$118.86 PnL on $750 capital (+15.8% return on deployed capital), Brier(LLM)=0.1501 vs Brier(market)=0.1607 (LLM beats market by 0.0106). Baseline all-BUY: 87 trades, 13.8% win rate, −$257.79 PnL on $435 capital (−59.3%).** Long-shot-only SELL filter grid (mid threshold × |Δ| threshold) — best risk-adjusted: **`mid<0.20 AND |Δ|≥0.05`: 17 trades, 100% win rate, +$13.78 on $85 capital (+16.2%), avg $0.81/trade**. Tighter `mid<0.20 AND |Δ|≥0.10`: 6 trades, 100% win, $1.08/trade avg. The previously-discussed "headline filter" (mid<0.15, |Δ|≥0.02) was too narrow on the edge threshold — including 2 losers dropped win rate to 90.5%. **Reconciles the event-level NO_GO with the strong PnL**: at event-level (128 distinct events) the Brier improvement is correlated-bucket noise, but at trade-level (150 SELL trades) the PnL is realized across many independent market resolutions where each correlated bucket is its own payoff. Calibration vs. PnL test different things — for trading purposes, PnL is what matters. **Strategic conclusion**: Claude has a real SELL-side edge on overpriced markets (especially mid<0.20 long-shots with |Δ|≥0.05 to market price); the BUY-side is anti-edge and should never be traded. The revised Stage C trade filter is: SELL signals only, mid<0.20, |Δ|≥0.05. This applies regardless of Monday's PGA verdict (the retro PnL evidence is independent of, and stronger than, the PGA verdict). Output: `data/llm_retro_filter_pnl.json`. **Strongest live SELL candidate passing the new filter**: Alex Smalley winner-SELL (mid=0.056, p_llm=0.003, |Δ|=0.053), resolves Mon 5/18 → natural test for first Stage C live trade.
- 2026-05-17 — Phase 5 / Stage B+ — **NO-token routing built; Billboard+Cannes widening; refined edge pattern hypothesis.** Three pieces of work this session.
  1. **NO-token mirror** (no API cost). Modified `deploy/llm_predict_markets.py::predict_market` to record `no_token_id` alongside `token_id` (YES) in each prediction row. Modified `strategies/llm_probability_strategy.LLMProbabilityStrategy._load_latest_prediction` to match on either the YES or NO token_id, and `generate_signal` to flip `p_llm → 1 − p_llm` (and the side direction) when matched on NO. Built `deploy/llm_backfill_no_token.py` to backfill the existing 75 predictions with `no_token_id` via Gamma's `condition_ids` filter (zero API spend). Backfilled all 75. Smoke-tested: Alex Smalley YES-token lookup → SELL @ 0.056 (blocked by BUY_ONLY_MODE); NO-token lookup → **BUY @ 0.944, edge +530 bps** (economically identical, executable). This unlocks SELL-side Stage A trades for Stage C without refactoring the trader or risk manager.
  2. **Widen with retail-bracket events** (~$0.40 API). Built `data/_find_retail_brackets.py` (deleted after use) that scans currently-open Polymarket events for retail-bracket structure (Cannes/Tony/Oscar/Eurovision/MVP/album-feature/Billboard keywords). Found 5 candidate events: Billboard Hot 100 May 23 (3 markets, $2.8K liq, **resolves 1.5 days**), Cannes Palme d'Or (3 markets, $2.5K liq, **resolves 5.5 days**), WNBA MVP (130 days, untradable spreads), Billboard 2026 (228 days), NFL MVP (273 days). Ran predictions on the 2 short-cycle events (4 new predictions written, total Stage B paper-track now **79 predictions** across 6 events: PGA / Elon-tweets / Monaco / NBA MVP / Billboard / Cannes).
  3. **Refined edge hypothesis from today's data**. The strongest new candidate signal is "Choosin' Texas — Ella Langley" Billboard #1 SELL: edge **-9695 bps** (Claude says p=0.02, market says p=0.99). But this is exactly the *anti-pattern* of Stage A: market is highly confident in a *near-certainty* (current #1 song staying #1 next week), Claude has no current chart data and just gives the long-tail "any specific song is unlikely" prior. Same logical failure as Rory McIlroy Top-20 SELL (market right at 0.585, Claude wrong at 0.50) and Wyndham Clark Top-20 BUY (Claude overconfident without current form data). Pattern emerging from today's resolutions: **Claude's edge is specifically selling overpriced LONG-SHOTS (mid < ~0.15) where retail bids up speculative tail buckets**. The reverse — Claude SELL on near-certainty, or Claude BUY on long-shot needing current form data — both lose because they require Claude to have *more* information than market, which it doesn't. **Stage C trade filter should restrict to: SELL signals where `market_mid < 0.15` AND `p_llm < market_mid - 0.02`**. Resolves tomorrow + Tuesday should confirm or refute this on the Billboard "Choosin' Texas" trade (a natural test of the anti-pattern).
- 2026-05-17 — Phase 5 / Stage A robustness — **Event-level dedup flips Stage A verdict to NO_GO; original GO was effectively n=2 events.** Built `deploy/llm_calibration_dedup.py` that re-aggregates the 240-row calibration JSONL by underlying event (slug-based with regex fallback for ICEMAN/Eurovision/Cerebras/etc), then computes Brier(LLM) vs Brier(market) with one observation per event. Result: **240 markets → 128 distinct events**. Two events dominated the original `awards_entertainment` GO: Drake ICEMAN album (46 feature buckets, 1 event) and Eurovision 2026 (44 brackets, 1 event). Both events independently showed LLM beating market at the event-level (ICEMAN: LLM 0.283 vs market 0.423 vs actual 0.217; Eurovision: LLM 0.250 vs market 0.272 vs actual 0.227), but **n_events=2 is below the gate_min_n=10 floor**, so the verdict is unsupported. Other niches at event-level: `sports_outright` n=76 Δ=+0.0001 (LLM ties market), `scheduled_event` n=42 Δ=+0.0111 (LLM loses). **Event-level overall verdict: NO_GO** (LLM Brier 0.1613 vs market 0.1563, Δ=+0.0050). **Implication**: the Stage A "GO" reflected real edge on 2 retail-bracket events but the sample is too thin to call generalization. Today's PGA early results (1W/4L paper PnL -$0.09 on 5 tradeable bets, sports_outright niche) are consistent with this: sportsbook-informed markets do NOT show the same edge. **Revised Stage C posture**: do not deploy capital just on PGA paper-PnL turning positive at final resolution; require additional independent retail-bracket events (Cannes 5/24, Tony Awards 6/8, future album releases, etc) to show the same pattern. Output: `data/llm_calibration_dedup_summary.json`.
- 2026-05-17 — Phase 5 / Stage B interim (PGA in progress) — **Resolver bug fixed; partial early results show LLM underperforming market.** Fixed `deploy/llm_resolve_predictions.py::fetch_market_by_condition_id` which was scanning ~20 pages of `/markets` per call (returning `not_found=69/75`). Gamma supports `condition_ids=<cid>&closed=true|false` as a direct filter — refactored to a 2-call lookup per cid. Runtime 6 min → 46 s, 0% not_found. Ran on current state: **11 of 75 resolved (Top-N PGA + Elon early-eliminations), 5 tradeable, paper PnL -$0.09 (1W/4L)**. Winner: ✅ Si Woo Kim winner-SELL +$0.18 (Stage A pattern held). Losers: Wyndham Clark Top-20 BUY -$0.18 (Claude overestimated mid-tier player without current form data — mirror of the elite-underestimation flag), Shane Lowry Top-5 BUY -$0.07, both Elon tweet-bucket BUYs -$0.02 combined. Brier(LLM)=0.0053 vs Brier(market)=0.0006 on this resolved subset. **Pattern hint emerging**: SELL-side long-shot fade (1/1 so far) is working; BUY-side mid-tier player picks (0/4) are losing — consistent with Stage A's actual mechanism. **Not yet calling B→C verdict**: 64 markets still unresolved, including the 2 strongest signals (Alex Smalley winner-SELL -530 bps, Hideki Matsuyama winner-SELL -360 bps, both resolve Mon 5/18). Re-run `python -m deploy.llm_resolve_predictions` after the PGA Championship final round Monday for the real verdict.
- 2026-05-17 — Phase 5 / Stage B widen + funding check — **Polymarket account verified ($4.66 USDC, 0 open orders); Stage B sample widened to 75 markets / 4 events.** Built `deploy/check_polymarket_funding.py` (read-only probe of L2 auth + USDC balance + open orders via `py-clob-client.get_balance_allowance` + `get_orders`) and `deploy/rederive_polymarket_creds.py` (non-destructive comparison of `.env` L2 creds vs what the server derives for the wallet's PRIVATE_KEY). First funding probe returned 401 — the post-migration `.env` carried stale API_KEY/SECRET/PASSPHRASE that didn't match what the server has registered for this wallet. Ran the rederive probe; it returned the correct triple, which I patched into `.env` (PRIVATE_KEY, FUNDER_ADDRESS, SIGNATURE_TYPE=2 unchanged). Re-running the funding probe with fresh creds: **`has_l2_auth=true`, USDC balance $4.66, allowances maxuint256 on all 3 Polymarket exchange contracts, 0 open orders, 2 historical trades on this wallet**. Status: ready to trade but **balance < $5 means Stage C must size at $1-4 per trade** (or top up). Also widened Stage B paper-track: ran `deploy/llm_predict_markets` on 3 more bracketed events — Elon Musk monthly tweet-count brackets (3 markets, 2 with quotes), F1 Monaco GP June 7 (2 markets with quotes), NBA Finals MVP (4 markets, only 1 with quotes). 9 new predictions logged. **3 new tradeable candidates above 300 bps edge**: Jalen Brunson NBA Finals MVP SELL (-545 bps, $9K liq, resolves ~June 17), Elon Musk 1280-1319 tweet-count bucket BUY (+385 bps, $9K liq, resolves June 1), Elon Musk 480-499 bucket BUY (+365 bps, $9K liq, resolves June 1). The Elon tweet-count brackets are an especially clean Stage-A-pattern test on a non-event count metric (different from albums/contests but same long-shot mispricing). Total Stage B paper-track now: 75 markets across PGA (resolves 5/17-18), Elon-tweets (5/31), Monaco GP (6/7), NBA MVP (~6/17). Cumulative API: ~$18. Live capital deployed: $0.
- 2026-05-16 — Phase 5 candidate #2 / Stage C wiring — **Live-trade infrastructure built (not yet executed).** Built `strategies/llm_probability_strategy.py` (subclass of `BaseStrategy`, reads `data/llm_predictions.jsonl`, looks up most-recent prediction for a `token_id` within `max_age_hours`, returns `StrategySignal(side, confidence, price, reason, metadata)` with `confidence = min(1.0, llm_confidence × |edge| × 5.0)`; no LLM calls inside the trade loop, per PLAN). Wired `--llm-mode` flag into `deploy/run_live.py` that swaps the default MACD/RSI/CVD/LongEntry ensemble for a single `LLMProbabilityStrategy`. Smoke-tested end-to-end: Alex Smalley winner-SELL prediction (p=0.003 vs mid=0.056) correctly produces `StrategySignal(side=SELL, confidence=0.225, price=0.056)` with metadata carrying edge_bps and reasoning excerpt. **Open routing issue blocking SELL-side Stage C trades**: `BUY_ONLY_MODE=true` + `RiskManager.validate_order` rejects "Order would create a net short position", so SELL signals from a no-inventory state get blocked. The Stage A pattern (and 21/25 PGA candidates) is **SELL the long-shot YES = BUY the NO**. To execute the strongest PGA candidates, either: (a) extend `deploy/llm_predict_markets.py` to also emit a mirrored prediction for the NO token (`p_no = 1 - p_yes`, `token_id = no_token_id`) so SELL-YES becomes BUY-NO via the same strategy, or (b) refactor `PolymarketTrader.build_order_decision` + `RiskManager` to accept "sell" without inventory and translate to NO-buy at the trader level. Option (a) is cleaner; will pursue post-Stage-B verdict. **For Stage C trade selection given the current SELL block**, only BUY candidates are tradeable. Top PGA BUY: Wyndham Clark Top-20 (p_llm=0.22, mid=0.036, edge +1840bps, liq=$932) — bridges to Stage C if Stage B verdicts GO. **Status**: ready to execute a BUY-only Stage C trade with one command (`python -m deploy.run_live --llm-mode --execute --condition-id <cid> --token-id <tid> --best-bid <b> --best-ask <a> --decision-ts <iso>`); SELL-side requires the (a)/(b) follow-up. No live capital yet deployed; PLAN's Stage B paper-track discipline maintained.
- 2026-05-16 — Phase 5 candidate #2 / Stage B start — **Forward predictions on 2026 PGA Championship (tournament resolves 2026-05-17/18) — paper-track only.** Built `deploy/llm_predict_markets.py` (forward driver with event-slug grouping, prediction cache, ranked candidate output) and `deploy/llm_resolve_predictions.py` (post-resolution Brier vs market_p_at_prediction + paper-PnL @ $5 size + mechanical B→C gate). Ran predictions on 66 PGA bucket markets across 4 brackets (Winner ×20, Top-10 ×19, Top-5 ×17, Top-20 ×14). API cost: ~$4.50. Result: **25 candidates above 300 bps edge, but only 3 are structurally clean trades** (Winner SELLs on outside contenders: Alex Smalley p_llm=0.003 vs mid=0.056 edge -530 bps liq=$89,940; Hideki Matsuyama p_llm=0.015 vs mid=0.051 edge -360 bps liq=$97,303; Si Woo Kim p_llm=0.005 vs mid=0.036 edge -310 bps liq=$8,473). The other 22 candidates split between: (a) tiny-liquidity Top-N bracket markets ($83-$98) with inactionable spreads, (b) Top-N SELLs on elite players (Rory McIlroy 50%, Ludvig Aberg 32%) where **Claude is systematically underestimating elites due to lack of current-form data — these would lose money if traded**. **Crucial Stage B insight already**: Claude's edge is on the long-tail fade (correctly says "very low" for outside contenders), not on mid-tier or elite bracket markets where current sportsbook data is needed. **User chose paper-track only** (plan-aligned, no capital at risk). When tournament resolves Sunday/Monday, `python -m deploy.llm_resolve_predictions` will compute realized Brier(LLM) vs Brier(market_p_t0) + paper PnL on each tradeable candidate at $5 hypothetical size, and fire the B→C gate (GO iff Brier(LLM) ≤ Brier(market) AND paper PnL > $0 on ≥3 tradeable bets). Output: `data/llm_predictions.jsonl` (66 rows). Outstanding: re-evaluate after PGA Championship resolution late 2026-05-18 UTC.
- 2026-05-16 — Phase 5 candidate #2 / Stage A — **LLM probability-engine: mechanical GO on awards_entertainment, but with structural caveats.** Built the full stack: `polymarket_rbi_bot/llm_prompts.py` + `llm_client.py` (Anthropic Claude Opus 4.7 primary via Messages API + `tool_use` forcing; OpenAI GPT-5.5 fallback via `response_format: json_schema`; retries on 429/5xx only; ~$0.065/call, ~9s latency, system prompt under cache threshold so no prompt-caching savings) + `deploy/llm_calibration_backtest.py` (Stage A driver with closed-market fetch, transient/horizon filters, T-7d price via `PolymarketHistoryClient`, per-niche Brier vs Polymarket price, mechanical A→B gate). Sanity test passed on 3 hand-picked markets (Boston Celtics p_yes=0.04 w/ Tatum injury rationale; Fed June cut p_yes=0.35 with explicitly low conf flagging post-cutoff; BTC $200k by Jun 30 p_yes=0.08). **Stage A run** on 240 resolved markets from Gamma `closed=true` Feb-May 2026 with volume ≥$1000 and ≥7-day horizon, after filtering 1500+ transient micro-markets (esports per-game, weather, intraday crypto). Per-niche Brier results (Claude's self-classification):
  - **`awards_entertainment` n=68: Brier(LLM)=0.1063 vs Brier(market_p_t7)=0.1269, Δ=-0.0206 → PASS** (gate: Δ ≤ -0.005 AND Brier(LLM) < 0.22 AND n ≥ 20)
  - `sports_outright` n=82: Brier(LLM)=0.1955 vs Brier(market)=0.1949, Δ=+0.0006 → essentially ties market
  - `scheduled_event` n=75: Brier(LLM)=0.1551 vs 0.0988, Δ=+0.0563 → LLM loses meaningfully
  - `corporate_event` n=10: below n=20 floor; skipped
  - All others n<3
  - **Verdict: GO with `awards_entertainment` niche.**

  **Honest caveats on the GO:** The 68 awards_entertainment markets are NOT 68 independent observations. 42 are "Will Drake feature [name] on ICEMAN?" markets from ONE album release; 26 are bracketed Eurovision-2026 advance/top-N markets. Effective independent sample is ~2-3 events. The Brier edge is concentrated in a specific pattern: **retail-overpriced low-probability bucket markets on bracketed/multi-outcome events** where Claude correctly says "low" on speculative long-shot buckets that retail bids up to 20-50%. Examples: market said 0.48 on "Will Drake feature Pimmie?" (LLM said 0.05, resolved NO); market said 0.62 on "Will Drake feature Julia Wolf?" (LLM said 0.18, resolved NO). Calibration curve is well-aligned in the low-probability bin (predicted 0.08 → empirical 0.05) and high-confidence bin (predicted 0.84 → empirical 0.75). **Implication for Stage B:** the niche to target is not "awards_entertainment" broadly — it's *correlated-bucket markets on speculative bracketed events* (album releases, song contests, awards predictions, possibly sports brackets). At $1-10 trade size and ~$100 cap, realistic opportunity is ~1-2 such event waves per month × 5-10 high-edge buckets per wave × $5 each = $25-50 deployed per wave, expected gross $1-3 per trade after costs. Roughly **$5-15/month gross at full saturation** — validation-level, not income. Outputs: `data/llm_calibration_backtest.jsonl` (240 rows), `data/llm_calibration_summary.json` (per-niche table + calibration curves). API cost: ~$13. Proceeding to Stage B (forward predictions on currently-open bracketed events, paper-trade for ≥4 days).
- 2026-05-13 — Phase 5 prep — **Maker quoting also effectively dead at this scale.** Built `deploy/simulate_maker_fills.py` (paper simulator over `data/quote_collection/run.jsonl`: posts hypothetical resting BUY at `bid + 1 tick` and SELL at `ask - 1 tick`, walks forward 30 minutes for fill, then 15 minutes more for adverse drift, records realized PnL vs mid). Ran across the full 2.6M-record / 16-day / 140-token corpus in 45 s. **Out of 140 tokens, only 5 have positive average buy-maker PnL bps and 3 have positive sell-maker PnL bps.** Per-token expected dollar PnL at $1 size, even posting at every 30s poll, is well under $0.10 across 16 days for every candidate. Best non-long-shot candidate: Boston Celtics NO (mid≈0.866, 32.7% fill rate × +1.2 bps × 6132 fills) → ~$0.07 over 16 days at $1/order. Long-shot tokens (mid<0.05) show high bps PnL because of small-denominator inflation but their absolute dollar PnL is similarly tiny; high adverse-drift bps confirm classic maker pickoff (e.g., Tulsi Gabbard YES: +4124 hs but -3857 adverse = +454 net bps, with `adverse_drift_bps` swinging on every poll). **Implications**: (a) Phase 5 maker-quoting was the last surviving edge candidate ranked in PLAN section 1 and it does not survive honest fill+adverse simulation on 16 days of real CLOB data; (b) the maker-feasibility memo's "lean negative" verdict from 2026-04-27 is now confirmed quantitatively across the full token universe; (c) committing 2–4 weeks of websocket/queue-position engineering to a strategy whose paper edge is sub-cents per day per token would be unwise. **Action**: marking Edge #3 (maker quoting) as `[-] abandoned, reason: 5/140 tokens net-positive at any meaningful level on 16 days of CLOB data; absolute PnL ~$0.07 over the window even on the best candidate. Cost (engineering time) >> expected income.` Output: `data/maker_fill_sim.json`. Net effect of today's work: Edges #1, #2, #3, #4 in PLAN section 1 are now all falsified or abandoned on this codebase + this venue at retail scale. Recommended user-facing posture: shelve live trading; keep the repo as research tooling per Section 3 off-ramp.
- 2026-05-13 — Phase 3 — **Structural arb formally retired: NO_GO under honest leg-cost accounting.** Closed the 2026-04-25 open question ("expand watchlist to full field and re-test"). Steps: (1) `expand_watchlist_outright --apply` reported `new_entries: 0` — Gamma's currently-active universe (6 NBA Finals, 7 NHL Cup, 2 EPL) is already fully captured in `data/scan_shortlist.json`; the "14-of-16" partial buckets in the 2026-05-11 scan reflect historically-collected condition-ids no longer in the active universe (eliminated teams), not a watchlist coverage gap. (2) Extended `deploy/scan_live_arb.py` with leg-level half-spread cost: `bundle_leg_cost_bps = sum((ask_leg - bid_leg) / 2 * 10000)` per bucket, denominator-matched to the bundle's $1 payout. `net_edge_bps` now reports leg-adjusted edge; gross is kept under `gross_edge_bps`. Also added `--restrict-to-watchlist` for filtering off stale legs. (3) Re-ran the championship scan on the full 16 days of CLOB data: **total opportunities ≥ 30 bps net edge: 0 with the dimensionally-incorrect first-cut formula** (it conflated per-leg-mid bps with per-bundle bps and produced absurd 8000+ bps leg costs); **46 with the corrected formula** — all sell-field NBA Finals, all clustered in a single 45-minute window 2026-04-28 15:28–16:13 UTC. Median NBA sell edge across the full 1387 qualifying buckets: -140 bps net (-10 gross - 130 leg cost). NHL: 0 opportunities, median -440 bps net. EPL: 0 opportunities. (4) `deploy/track_a_decision.py` returned **NO_GO** on all 3 championships against the pre-committed 2026-04-25 gates. **Implications**: (a) the apparent 220 bps "edge" from the 2026-05-11 scan was gross-edge-only — leg half-spreads consume ~110-130 bps of it, leaving ~95 bps gross-positive only on the single anomalous 45-min window; (b) outside that one event, the bundles are structurally over-priced (median sell edge negative); (c) at the user's $100 cap, a $15 NBA-Finals bundle on that single window would have paid ~$0.14 once — not a strategy. **Action**: marking structural-arb Edge #1 as `[-] abandoned, reason: no leg-adjusted survivor at full coverage on 16 days of data; opportunities found are one-time book events, not persistent.` Next: run the passive-fill simulator on existing JSONL to inform whether the Phase 5 maker-quoting build is worth scoping, then decide between shelving live trading entirely vs. committing engineering time to a maker bot. **No live capital deployed.** Off-ramp from this plan's Step 6 (live $15 pilot) is engaged.
- 2026-05-11 — Phase 1.5 / Track A tooling — **Post-migration decision path repaired.** `deploy.scan_live_arb` now supports `--json-out` and its edge summaries include `p05` as well as median/min/max/p95, which lets the Track A criterion "p95 bundle ask below threshold" be evaluated as "5th percentile buy edge clears threshold" on the current edge-based report. `deploy.track_a_decision` now accepts the current `per_championship` scan shape while retaining compatibility with the older `championships`/bundle-ask shape. Smoke check: migrated `data/scan_shortlist.json` has 100% field coverage for NBA Finals 2026, NHL Cup 2026, and EPL 2025-26. **Next required action**: run `python -m deploy.scan_live_arb --championship-mode --json-out data/track_a_scan_2026_05_11.json`, then `python -m deploy.track_a_decision --scan-output data/track_a_scan_2026_05_11.json --shortlist data/scan_shortlist.json`. Do not advance to live until this verdict is recorded.
- 2026-05-11 — Migration / Phase 1 ops — **Windows/WSL bring-up path added.** Added `deploy/install_windows_wsl_collectors.ps1` plus `deploy/windows_wsl_bringup.md` to replace the macOS LaunchAgents with NSSM-supervised WSL collectors. WSL migration check found the core corpora present (`data/quote_collection/run.jsonl` ~2.1 GB, `data/quote_collection/nonsports_run.jsonl` ~418 MB), but both streams are stale: last observed rows are 2026-05-04 UTC. Track A's pinned verdict date (2026-05-02) is now historical, so new collection cannot fill that decision window; it is only useful for the next research cycle. Main watchlist `data/scan_shortlist.json` is present. Nonsports output survived, but `data/scan_shortlist_nonsports.json` is missing, so the nonsports collector should either wait for the original Mac backup file or be deliberately regenerated as a fresh 2026-05-11-era corpus. **Expected impact**: restart collection on Windows without cloud-syncing secrets, preserve the append-only JSONL corpora, and make the next profitable-bot step concrete: run the frozen Track A/post-Track A analysis, then choose between full-field arb expansion, passive-fill simulator, or shelving live trading until a real edge clears costs.
- 2026-04-27 — Phase 5 prep — **Maker-quoting feasibility memo drafted** (`data/maker_quoting_feasibility_memo.md`). Aggregated existing evidence rather than running new probes: (a) 2 live post-only orders (Spurs, Lightning) both rested cleanly with **0 fills** and 240–360 bps spreads; (b) 3 fill-likelihood snapshots (8/10/24 markets) show **89–100% never-touched at-touch** and **66–67% never-touched one-tick-inside** within 10–20s windows on the universes we tested. **Verdict**: lean negative on a generic maker bot in current configuration; conditionally positive on a niche subset that has yet to be identified. **Recommendation order**: do not start the build before Track A's 2026-05-02 verdict; if Track A is NO_GO, the next step is a 2–3 day passive-fill simulator on existing collector data to *rank* markets by expected maker edge before committing 3 weeks of engineering. Memo also flags four open questions to resolve at decision time (Polymarket fee schedule today, websocket reliability for queue position, cancel-replace rate limits, tick-size grid). **Why now**: we're inside the 5-day Track A idle window; this front-loads the Phase 5 question so we don't make decision-day decisions about Phase 5 hot.
- 2026-04-27 — Phase 1.5 — **Track A decision script rehearsed; INSUFFICIENT_DATA path added.** Walked the four real verdict cases through `deploy/track_a_decision.py` against synthetic fixtures (full-coverage GO, full-coverage NO_GO, 11/16 partial AMBIGUOUS, 2/16 thin INSUFFICIENT). Rehearsal exposed a real bug: championships with 0% coverage (because the synthetic shortlist had no entries for them) were being flagged AMBIGUOUS, which was escalating an otherwise-clean NO_GO to overall AMBIGUOUS. Fix: added `MIN_FIELD_COVERAGE_USABLE = 0.30` floor and a new `INSUFFICIENT_DATA` verdict that excludes a championship from the overall verdict rather than poisoning it. All four cases now route correctly. **Why this mattered**: if we'd run this fresh on 2026-05-02, the wrong overall verdict could have been recorded under time pressure. Front-loading the rehearsal in the idle window caught it cold.
- 2026-04-27 — Phase 2 — **Non-sports corpus collection started.** PLAN's open Phase-2 question ("before declaring directional Edge #2 falsified globally we need a non-sports CSV corpus") is now in motion. Built `deploy/discover_nonsports_corpus.py` which classifies live Gamma markets via `MarketFilter._heuristic_family` and writes a top-N-per-family shortlist. Discovered 32 markets ≥ $20K liquidity across 4 non-sports families: legal_regulatory (8), news_breaking (8) including Fed-cut and ceasefire markets, scheduled_event (8) including 2028 Dem primary candidates, event_resolution (8) including Hungary PM. Crypto family came back empty — heuristic requires both crypto + outright keywords; few candidates passed. Wrote `data/scan_shortlist_nonsports.json` and a parallel LaunchAgent `com.polymarket.rbi-bot.quote-collector-nonsports` that polls these into `data/quote_collection/nonsports_run.jsonl`. **Architecture choice**: kept this as a *second* collector (separate plist + JSONL), not merged into the main 72-condition watchlist. Reason: Track A's 2026-05-02 verdict depends on clean coverage of the sports field; isolating risk surface keeps a non-sports collector failure from compromising the structural-arb decision data. **Quality caveat**: smoke test showed 100% of rows came from `gamma_markets_poll` source rather than CLOB order book — the non-sports markets have sparse books on average. This is fine for honest-test gate (mid-only is enough to compute signal Brier vs base-rate), but means we will *not* have executable quote depth for these markets if Phase 3 ever wants to trade them. Also extended `deploy/collector_health.py` with `--stream {main,nonsports}` and parameterized paths so the dashboard now shows dual-stream health. Plan: let it accumulate ≥5 days, then re-run the honest-test gate (Phase 2 Brier vs base-rate) on a non-sports CSV export. Earliest re-test ~2026-05-02, same calendar slot as Track A decision.
- 2026-04-25 — Phase 2 — **LongEntry signal is dead on this corpus, not just gated.** Built `deploy/audit_long_entry_confidence.py` (per-bar `LongEntryStrategy(strict_mode=True, signal_version=v2)` walk, full confidence histogram, no engine gates). Across 21,880 bars × 10 sports CSVs, `LongEntry` returned `BUY` exactly **0 times** and `SELL` 0 times — 100% HOLD. So the "0 trades from every strict_* experiment" finding from earlier today is *not* a threshold-too-tight problem; the strategy simply doesn't propose entries at any confidence level. Cause is the corpus: avg absolute per-bar return is **1.2–5.2 bps** with total price ranges of 248–1901 bps over ~1.5 days — well below LongEntry's strict momentum thresholds (which look for tens of bps cumulative move across multi-bar windows). This is exactly what the 99% mid-staleness finding predicted: nothing for a directional momentum signal to lock onto. **Implications**: (a) tuning `MIN_ENTRY_CONFIDENCE` is moot on this corpus — the signal never proposes; (b) `loose_baseline`'s 100+ trades came from MACD/RSI co-equal voting in non-strict mode, and as Phase 2's honest-test gate showed, those weren't skill — they were noise getting picked off (adverse_fill_ratio=1.0); (c) any productive directional work needs *different markets* (more inherent volatility) or a *non-momentum* strategy class (mean-reversion, liquidity provision); (d) cost-prior + LongEntry-dead together strongly support the thesis that this venue at this scale doesn't sustain directional edges, and reinforce the structural-arb-or-bust framing for Phase 1.5.
- 2026-04-25 — Phase 1 — **Cost prior was off by ~2x.** Audited `cost_attribution` from the same 296 round-trips: trade-weighted average entry spread 113.2 bps, exit spread 104.5 bps; adding 25 bps slippage per side (env default) gives an **observed round-trip cost of 158.9 bps**, not the 80 bps `ESTIMATED_ROUND_TRIP_COST_BPS` prior. `adverse_fill_ratio = 1.00` and `average_post_fill_move_bps = -122.9 bps`: every single fill in the sample was followed by an adverse move, i.e. the signal stack is being systematically picked off. Gross PnL across the 296 trades was barely positive ($+1.59); net was $-11.30 — costs ate everything. **Implications**: (a) every prior falsification used a 30/100 bps MVE bar that was *less* than actual cost, so the negative findings are even more decisive than they read; (b) the strict-mode `edge_vs_cost` gate has been letting through trades that would only break even if costs were what we *assumed*, not what they are; (c) maker-quoting (Phase 5 candidate) becomes more attractive in this light because *we* would be capturing the half-spread instead of paying it. **Action**: bumped `ESTIMATED_ROUND_TRIP_COST_BPS` 80 → 160 in `.env` so the strict gate now requires real edge. Also added a crossed-quote block to `RiskManager.evaluate_execution_guards` (was silently passing because `(ask − bid)/mid` goes negative on crossed quotes and `> max_spread_bps` returns False).
- 2026-04-25 — Phase 2 — **Honest-test gate run on the existing 10 sports CSVs.** Result: directional signal stack is **uncalibrated and uninformative** on this corpus. Trade-weighted Brier across 296 round-trips on `loose_baseline` (the only profile that actually trades): signal=0.0717, always-base-rate=0.0349, always-p=0.5=0.2500. Signal beats coin-flip but *loses to always-predicting-the-historical-hit-rate by +0.0368/trade* — the model's miscalibration costs more than its directional edge gains. Per-CSV: 0 of 10 CSVs have signal Brier < base-rate baseline; 2 tie (within ε), 8 lose. Two CSVs (56814725, 76502490) hit a floor 0.5000 because they only made 2 trades each at high confidence on the wrong direction. **All 8 strict_* experiments produced 0 trades** — entry gates reject everything on this universe, consistent with the 2026-04-23 finding that mids are frozen 99% of the time on these tokens. **Caveats**: small N (most CSVs <60 trades), all sports outrights, no mixed-family comparison, signal "confidence" here is `long_entry_confidence` not a true probability output. **Implication for PLAN edge ranking**: directional Edge #2 looks dead on the same universe where structural arb died. Before declaring it falsified globally we need (a) a non-sports CSV corpus and (b) a strict profile that actually trades. Otherwise Phase 2 result aligns with the 99% mid-staleness prior — there's nothing for directional signals to predict.
- 2026-04-23 — Phase 2 — Track B plumbing **shipped**. `polymarket_rbi_bot/calibration.py` (Brier + reliability bins, stdlib-only); `BacktestEngine` accepts `market_filter` + `market_metadata` and short-circuits to a zero-trade result with `skipped_by_family_filter=true` when the family/keyword gates reject; `MarketFilter.evaluate_family_only` runs gates without a history fetch; `data/csv_metadata.py` reads metadata from quote-backtest CSV row-0 (no Gamma round-trip needed — CSVs already carry question/condition_id/market_family/endDate); `run_experiment_matrix` and `run_walk_forward` accept `--family-filter {off,on,both}`, build `MarketFilter` from `BotConfig.from_env()`, classify family via the filter when CSVs lack the column, and emit per-family `by_family` block + per-run `brier_score`. **Smoke** on 10 quote-backtest CSVs × 9 experiments × 2 modes = 180 runs: filter classifies all 10 markets as `sports_outright` (allow-listed → 0 skipped → `filter_on` ≡ `filter_off` for this set, as expected). Brier sanity passes: `[0.5]×1000` against random outcomes → 0.25; sharply aligned 0.9/0.1 → 0.01. Per-CSV Brier ranges from 0.021 (sharp & accurate) to 0.5 (chance-level) on `loose_baseline`. **Implication**: family/keyword filter doesn't bind on this CSV set (all sports outrights), so its real test happens once we run on a mixed-family corpus or once `STRICT_STRATEGY_MODE=true` activates the strict-mode keyword list. Brier is now a first-class headline metric — Phase 2 honest-test gate is wired.
- 2026-04-25 — Phase 3 — **Primary edge falsified on this universe.** Built `deploy/scan_live_arb.py` and ran it against 5 days / 292,316 aligned 60-second buckets across all 50 binaries: after filtering 924 crossed-quote rows (0.08% data corruption from CLOB book races), **zero** Yes+No bundles cleared the 30 bps MVE. Distribution: bundle_ask ∈ [1.0000, 1.1410], bundle_bid ∈ [0.8590, 1.0000], median both 1.001/0.999 — Polymarket maintains tick-perfect $1 invariant on liquid binaries. The Gamma `--mode live` reference scan (500 markets) finds 0 non-balanced because Gamma normalizes outcomePrices to sum to 1 by construction (not a real arb signal). Cross-condition outright probe (sum of all NBA Finals YES asks): 0.962 median across 466 buckets — looks like 380 bps arb but is actually coverage artifact (we track 13 of 30 NBA teams; missing field would close the gap). Implications: (a) single-binary bundle arb is dead at our scale on liquid markets; (b) full-field outright arb is the only remaining structural angle and requires expanding the watchlist to all contenders per championship; (c) before committing more code to Phase 3 we need an honest re-read of edge ranking. **Next step decision pending**: either expand watchlist to full NBA + NHL + MLB outright fields and re-test, or accept that structural arb is gone for our scale and pivot edge ranking. Crossed-quote anomaly also worth filing separately — it's a CLOB book-race data-quality issue the live trader would need to handle.
- 2026-04-23 — Phase 1 — Coverage diagnostic at day 3 (`deploy/analyze_quote_coverage.py`, 830,500 rows / 100 tokens / 73.5h): 99.05% bid+ask coverage, 100% CLOB source, spread_bps median 63 / p75 465 / p95 1818 (heavy right tail), token staleness ratio median 0.995 (mids almost never move between 30s polls). Prune filter (p75 spread < 500 bps AND staleness < 0.95) keeps **12 of 100 tokens** — that's the Phase-2 trading universe candidate. Implications: (a) reinforces arb-first thesis — persistent mispricings, not random walks; (b) **kills directional signals** — nothing for MACD/RSI/CVD to work on when mids are frozen 99% of the time; (c) universe must be pruned hard before Phase 2. Next check-in: **2026-05-11** (Phase 2 open at ~3-week mark). Skipping a 2026-04-27 intermediate look — the structural story is already clear and another weekly re-read re-observes the same thing.
- 2026-04-20 — Plan refinement — Absorbed useful pieces of an "AI-as-probability-engine" framework: (1) added Brier-score/calibration check to Phase 2 (a stack that is often right on direction can still lose money with poorly calibrated probabilities); (2) expanded Phase 5 second-edge candidates to rank maker-quoting above LLM-probability-engine, with explicit scope + niche selection for the LLM track. Declined to restructure PLAN around LLM-as-primary because (a) competitors on news-driven markets are fast and informed, (b) we don't have news/evidence infrastructure, (c) it doesn't solve the liquidity/capacity ceiling. Arb-first stance unchanged.
- 2026-04-20 — Phase 1 — Cost model + paper log schema **shipped**. `BacktestEngine` now emits per-trade cost decomposition (spread / slippage / fee / post-fill-move / notional) and a run-level `cost_attribution` block (gross vs net PnL, USD cost buckets, adverse-fill ratio). `run_backtest` + `run_experiment_matrix` surface it. Paper log now carries `best_bid`/`best_ask`/`observed_spread_bps`/`schema_version=1` on every entry. Validated on `data/ceasefire_yes.csv`: 70 fills, $3.29 total costs split correctly, 82.8% adverse-fill ratio (flags this as a bad regime for the signal stack — exactly the Phase 2 insight we want).
- 2026-04-20 — Phase 0 — `.env` reconciliation: config loads clean, creds populated, not in git history. Open question on `SIGNATURE_TYPE=2` — user to confirm Polymarket account type.
- 2026-04-20 — Phase 1 — Quote collector **running**. LaunchAgent active under venv python `/Library/Frameworks/Python.framework/Versions/3.13/bin/python3.13` (FDA granted). Collecting 100 tokens @ 30s interval → `data/quote_collection/run.jsonl`. Clock starts now on the ≥3-week accumulation window. Next review: ~2026-05-11.
- 2026-04-20 — Phase 1 — Quote collector plumbing ready. Shortlist built (`scan_markets --limit 200 --top 50` → 50 condition-ids, 47 A-tier). Smoke test: 200 rows, 100% CLOB order-book source, all bid/ask/mid/spread fields present. LaunchAgent plist at `deploy/com.polymarket.rbi-bot.quote-collector.plist`. Initial TCC issue (`~/Desktop` is sandbox-protected) resolved by switching plist off bash wrapper and granting FDA to the venv python binary directly (narrower blast radius than bash FDA).
- 2026-04-20 — Phase 0 — MVE locked: arb ≥ 30 bps / directional ≥ 100 bps per round-trip after costs. Risk capital set at $500.
- 2026-04-20 — Phase 0 — Plan drafted.
